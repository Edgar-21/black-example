name: black
on: 
  pull_request:
  workflow_dispatch:
jobs:
  black:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - uses: psf/black@stable
        with:
        options: "--check --verbose"
        src: [".", "./dir1", "./dir2"]
